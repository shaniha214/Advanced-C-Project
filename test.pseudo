
treeNode * helper(boardPos start, boardPosArray ** moves, int matrix[N][M]);
makeList(boardPos * moves, int matrix[N][M]);

function Q3(boardPos start, moves, board) {
  boardPosArray = Q1(moves, board);
  matrix = 0;
  matrix[start] = 1;
  helper(start, boardPosArr, matrix);
}

function helper(start, boardPosArray, matrix) {
  newNode = createNewNode(start)
  newNode->children = makeList(boardPosArray[TOMOVE(start)], matrix) 

  for (child in list)
    helper(child, boardPosArray, matrix)

  return newNode;
}

function makeList(children, matrix) {
  for (node in children) 
    if (matrix[node] is 0)
      appendToList(node)
      matrix[node] = 1;
}



